#!/bin/sh

echo " * Launching Rserve daemon ..."

RSERVE_SCRIPT_HOME=/usr/lib/R/bin/

start-stop-daemon --start --chdir $RSERVE_SCRIPT_HOME --chuid <%= node[:R][:rserve_user] %> --exec $RSERVE_SCRIPT_HOME/Rserve.sh > /var/log/Rserve.log 2>&1 &

echo " * Rserve daemon running ..."

exit 0